services:
  - type: worker
    name: ethio-educational-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python books_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
